{
  "name": "livecut-desktop",
  "productName": "LiveCut",
  "version": "1.0.3",
  "description": "Thin Electron shell for LiveCut — loads the deployed site and defeats CORS/403 at the network layer.",
  "author": "Christopher Sabato",
  "private": true,
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently -k \"npm --prefix .. run dev\" \"wait-on tcp:5173 && cross-env LIVECUT_DEV=1 electron .\"",
    "build": "electron-builder --config electron-builder.yml --publish never",
    "build:mac": "electron-builder --mac --config electron-builder.yml --publish never",
    "build:win": "electron-builder --win --config electron-builder.yml --publish never"
  },
  "devDependencies": {
    "concurrently": "^9",
    "cross-env": "^7",
    "electron": "^44.1.1",
    "electron-builder": "^26.15.3",
    "wait-on": "^8"
  }
}
